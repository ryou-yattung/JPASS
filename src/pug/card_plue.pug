doctype html
html(lang="ja")
     head
          include _html3_01_head.pug
          link(rel="stylesheet" href="css/card_plue.css")
          title 暗記カード

     body 
          header 
               h1 暗記カード

          main 
               .wrap 
                    .wrap_title 
                         .wrap_title_gam.wrap_title_both
                              h2 文法
                         .wrap_title_word.wrap_title_both
                              h2 言葉

                    .wrap_input 
                         form(action="", method="post") 
                              .wrap_input_bg
                                   p 正面
                                   input(type="text")
                              .wrap_input_bg
                                   p 背面
                                   input(type="text")

                              .wrap_input_conf  
                                   a(href="card.html") 
                                        p 確認

          include _footer.pug

          include _jquery.pug
          script(src="js/study.js")  